% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flex-print-tidytableone.R
\name{flex_print_tidytableone}
\alias{flex_print_tidytableone}
\title{Format a tidytableone summary table with indentation-aware padding}
\usage{
flex_print_tidytableone(tidy_t1, column1 = "var", padding = 20, ...)
}
\arguments{
\item{tidy_t1}{A data frame or tibble returned by \code{create_tidy_table_one()} and optionally modified
by \code{adorn_tidytableone()}.}

\item{column1}{Character. The name of the column to check for indentation (default: \code{"var"}).}

\item{padding}{Numeric. Amount of left padding (in pts) to apply to indented rows (default: 20).}

\item{...}{Additional arguments passed to \code{lamisc::flexprint()}.}
}
\value{
A \code{flextable} object with formatting and conditional left padding applied.
}
\description{
This function formats a summary table created with \code{tidytableone} using \code{lamisc::flexprint()} and adds
left padding to rows based on indentation in a specified column (e.g., to visually group variables).
}
\examples{
\dontrun{
t1 <- tidytableone::create_tidy_table_one(data = baseline_df, vars = table1_vars)
t1 <- tidytableone::adorn_tidytableone(t1)
t1 <- dplyr::rename(t1, Characteristic = var)
flex_print_tidytableone(t1, column1 = "Characteristic")
}
}
