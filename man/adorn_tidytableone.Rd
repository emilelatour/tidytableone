% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adorn-tidytableone.R
\name{adorn_tidytableone}
\alias{adorn_tidytableone}
\title{Format tidytableone for display and reporting}
\usage{
adorn_tidytableone(
  tidy_t1,
  default_continuous = "{mean} ({sd})\\n{median} [{range}]",
  default_categorical = "{n} ({p})",
  fmt_vars = NULL,
  con_accuracy = 0.1,
  cat_accuracy = 0.1,
  p_accuracy = 0.001,
  prefix = "",
  suffix = "",
  big_mark = "",
  decimal_mark = ".",
  style_positive = c("none", "plus"),
  style_negative = c("hyphen", "minus", "parens"),
  scale_cut = NULL,
  con_trim = TRUE,
  cat_trim = FALSE,
  show_pct = TRUE,
  exact = NULL,
  nonnormal = NULL,
  equal_variance = NULL,
  no_cont_correction = NULL,
  monte_carlo_p = NULL,
  show_test = FALSE,
  show_smd = FALSE,
  use_labels = TRUE,
  combine_level_col = TRUE,
  missing = "no",
  missing_text = "(Missing)",
  default_miss = "{n}",
  checkbox_p = c("none", "per_level"),
  checkbox_p_adjust = "none",
  checkbox_block_p = c("any", "min"),
  ...
)
}
\arguments{
\item{tidy_t1}{Results in a tibble from \code{create_tidy_table_one}}

\item{default_continuous}{A glue statement that provides the formatting for
continuous variables, Default is \code{"{mean} ({sd})"}}

\item{default_categorical}{A glue statement that provides the formatting for
categorical variables, Default is \code{"{n} ({p})"}}

\item{fmt_vars}{A list of variable names and glue statements to override the
defaults for specific variables.}

\item{con_accuracy}{A number to round to for continuous variables. Use (e.g.)
0.01 to show 2 decimal places of precision. Default is 0.1.}

\item{cat_accuracy}{A number to round to for categorical variables. Use
(e.g.) 0.01 to show 2 decimal places of precision. Default is 0.1.}

\item{p_accuracy}{A number to round to for p-values and smd (when shown).
Use (e.g.) 0.01 to show 2 decimal places of precision. Default is 0.001.}

\item{prefix}{Additional text to display before the number. The suffix is
applied to absolute value before style_positive and style_negative are
processed so that prefix = "$" will yield (e.g.) ⁠-$1⁠ and ⁠($1)⁠.}

\item{suffix}{Additional text to display after the number.}

\item{big_mark}{Character used between every 3 digits to separate thousands.}

\item{decimal_mark}{The character to be used to indicate the numeric decimal point.}

\item{style_positive}{A string that determines the style of positive numbers.
See \code{?scales:number} for more information.}

\item{style_negative}{A string that determines the style of negative
numbers. See \code{?scales:number} for more information.}

\item{scale_cut}{Named numeric vector that allows you to rescale large (or
small) numbers and add a prefix. See \code{?scales:number} for more information.}

\item{con_trim}{Logical, if FALSE, values are right-justified to a common width (see \code{base::format()}).}

\item{cat_trim}{Logical, if FALSE, values are right-justified to a common width (see \code{base::format()}).}

\item{show_pct}{Logical, if FALSE, "\%" is omitted from percentages.}

\item{exact}{String vector of variable names to use exact tests for p-values.}

\item{nonnormal}{String vector of variable names to use non-parametric tests for p-values.}

\item{equal_variance}{String vector of variable names to assume equal variance.}

\item{no_cont_correction}{String vector of variable names to assume continuity correction.}

\item{monte_carlo_p}{String vector of variable names to simulate p-values.}

\item{show_test}{Logical, if FALSE, the names of the test are omitted from the table.}

\item{show_smd}{Logical, if FALSE, Standardized Mean Differences (SMD) are not included.}

\item{use_labels}{Logical, if TRUE, labels are used instead of variable names.}

\item{combine_level_col}{Combines the \code{var} and \code{level} columns into one instead
of two. HTML won't recognize the extra spaces when printing, but you can
used \code{flextable::padding} to indent the right rows in that column later.}

\item{missing}{Indicates whether to include counts of NA values in the table.
Allowed values are "no" (shows a column of number not missing), "ifany" (only display if
any NA values), and "always" (includes NA count row for all variables).
Default is "no".}

\item{missing_text}{Character string to use in place of \code{NA} when missing is
"ifany" or "always". Default is "(Missing)".}

\item{default_miss}{A glue statement that provides the formatting for
missing, Default is \code{"{n}"}}

\item{checkbox_p}{Logical; show per‑level checkbox p‑values column (default FALSE).}

\item{checkbox_p_adjust}{Character; p.adjust method for checkbox p‑values (default "none").}

\item{checkbox_block_p}{Logical; show one overall p‑value per checkbox block (default FALSE).}

\item{...}{Additional arguments. Not used.}
}
\value{
A tibble
}
\description{
Take the raw summary statistics from \code{create_tidy_table_one} and format them
for inclusion in a presentable table.
}
\examples{
library(dplyr)

dplyr::glimpse(pbc_mayo)

tab1 <- create_tidy_table_one(data = pbc_mayo,
                              strata = "trt",
                              vars = c("time",
                                       "status",
                                       "age",
                                       "sex",
                                       "ascites",
                                       "hepato",
                                       "spiders",
                                       "edema",
                                       "bili",
                                       "chol",
                                       "albumin",
                                       "copper",
                                       "alk_phos",
                                       "ast",
                                       "trig",
                                       "platelet",
                                       "protime",
                                       "stage"))

dplyr::glimpse(tab1)

# Check to see if assumptions may be violated
check_tests <- calc_table_one_tests(tab1)


dplyr::distinct(tab1, var)


adorn_tidytableone(tidy_t1 = tab1)

adorn_tidytableone(tidy_t1 = tab1,
                   show_test = TRUE)


adorn_tidytableone(tidy_t1 = tab1,
                   show_test = TRUE,
                   exact = "status",
                   nonnormal = check_tests$non_normal_shapiro)
}
