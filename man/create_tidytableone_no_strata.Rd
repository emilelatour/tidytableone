% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_tidy_table_one_no_strata.R
\name{create_tidytableone_no_strata}
\alias{create_tidytableone_no_strata}
\title{Create a tidy “Table 1” without strata}
\usage{
create_tidytableone_no_strata(
  data,
  vars,
  na_level = "(Missing)",
  b_replicates = 2000,
  ...,
  checkbox = NULL,
  checkbox_opts = NULL
)
}
\arguments{
\item{data}{A data frame with the variables to summarise.}

\item{vars}{Character vector of column names to include. If omitted,
all columns of \code{data} are considered.}

\item{na_level}{Character label for missing values in categorical
summaries. Default is \code{"(Missing)"}.}

\item{b_replicates}{Integer; number of bootstrap replicates for select
diagnostics (reserved for future use). Default \code{2000}.}

\item{...}{Reserved for future arguments; currently ignored.}

\item{checkbox}{Must be \code{NULL} for this legacy variant (checkbox blocks are
not handled here).}

\item{checkbox_opts}{Ignored (present for API symmetry).}
}
\value{
A tibble with the same columns as \code{\link[=create_tidy_table_one]{create_tidy_table_one()}}, e.g.:
\code{strata_var}, \code{strata}, \code{var}, \code{level}, wide continuous summaries
(\code{n}, \code{mean}, \code{sd}, \code{p25}, \code{p50}, \code{p75}, …), tall categorical summaries
(\code{n_level}, \code{pct}, \code{pct_valid}, …), testing columns present but \code{NA}
(since there are no groups), and metadata (\code{class}, \code{var_type}, \code{label}).
}
\description{
Produce the same schema as \code{\link[=create_tidy_table_one]{create_tidy_table_one()}} but with a single
overall group (every row has \code{strata = "Overall"}). No between‑group tests
are computed in this variant. This \emph{legacy} function does \strong{not} process
checkbox (multi‑response) blocks — see
\code{\link[=create_tidytableone_no_strata_checkbox]{create_tidytableone_no_strata_checkbox()}} for that.
}
\examples{
# Minimal example with one continuous and one categorical variable
set.seed(2)
df <- data.frame(
  age  = round(rnorm(100, mean = 50, sd = 10), 1),
  sex  = sample(c("Female", "Male"), size = 100, replace = TRUE),
  misc = rnorm(100)  # ignored below
)

tab <- create_tidytableone_no_strata(
  data = df,
  vars = c("age", "sex")
)
head(tab)

# Continuous only
tab_num <- create_tidytableone_no_strata(
  data = df,
  vars = "age"
)
head(tab_num)

# Categorical only
tab_cat <- create_tidytableone_no_strata(
  data = df,
  vars = "sex"
)
head(tab_cat)

# (If available) present like a traditional Table 1
# adorn_tidytableone(tab)

}
\seealso{
\code{\link[=create_tidy_table_one]{create_tidy_table_one()}}, \code{\link[=create_tidytableone_no_strata_checkbox]{create_tidytableone_no_strata_checkbox()}},
\code{\link[=adorn_tidytableone]{adorn_tidytableone()}}
}
